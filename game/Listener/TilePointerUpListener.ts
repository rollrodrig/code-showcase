import IListener, { EventType } from "./IListener";
export default class TilePointerUpListener implements IListener {
    listen(eventType:EventType, data:any): void {
        // todo, implement this.
    }
}
